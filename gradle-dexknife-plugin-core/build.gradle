buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
}

apply plugin: 'groovy'
apply plugin: 'idea'

repositories {
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}
